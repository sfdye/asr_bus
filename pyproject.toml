[project]
name = "asr-bus"
version = "0.1.0"
description = "Telegram bot for ASR shuttle bus timings"
requires-python = ">=3.11"
dependencies = [
    "python-telegram-bot>=22.7",
    "python-dotenv>=1.2.2",
]
